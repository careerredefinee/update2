# Render Deployment Environment
# Note: On Render, PORT is provided by the platform. Do not set PORT manually.

# Runtime
NODE_ENV=production
# PORT is set by Render automatically
# PORT=

# Database (Atlas or other managed MongoDB)
MONGO_URI=

# URLs / CORS
# Public URL of your Render service (backend also serves the built frontend)
# Example: https://your-app.onrender.com
FRONTEND_URL=
CORS_ALLOW_ALL=false

# Cookie domain (optional). For a custom domain, set to your apex domain.
# Example: COOKIE_DOMAIN=yourdomain.com
COOKIE_DOMAIN=

# Auth / Session
JWT_SECRET=
JWT_EXPIRES_IN=30d
SESSION_SECRET=
SESSION_ENCRYPTION_KEY=

# Cloudinary (optional)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Email (optional: SMTP)
EMAIL_FROM="careerredefine <noreply@careerredefine.com>"
EMAIL_FROM_NAME=CareerRedefine
EMAIL_HOST=smtp.gmail.com
EMAIL_PASSWORD=
EMAIL_PASS=

# Frontend (Vite) - API base
# If backend serves the frontend on the same origin, you can omit VITE_API_URL.
# Otherwise set to your backend Render URL.
# Example: VITE_API_URL=https://your-app.onrender.com
VITE_API_URL=

# Google OAuth
# Backend
GOOGLE_CLIENT_ID=
# Frontend
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_CLIENT_SECRET=

# Optional external integrations
NAUKRI_N8N_URL=
